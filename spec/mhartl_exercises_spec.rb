require "spec_helper"

RSpec.describe MhartlExercises do
  it "has a version number" do
    expect(MhartlExercises::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
